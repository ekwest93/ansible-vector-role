---
- name: Converge
  hosts: all
  become: yes
  become_method: su
  gather_facts: no
  
  pre_tasks:
    - name: Install Python
      raw: dnf install -y python3
      changed_when: false
    
    - name: Gather facts
      setup:

  roles:
    - role: vector
