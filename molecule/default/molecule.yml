---
driver:
  name: docker
platforms:
  - name: instance
    image: rockylinux:8
    pre_build_image: true
    command: /bin/bash -c "sleep infinity"
provisioner:
  name: ansible
verifier:
  name: ansible
